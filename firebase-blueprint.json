{
  "entities": {
    "Score": {
      "title": "Score",
      "description": "A leaderboard entry for Infinite Mode.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Player's name."
        },
        "score": {
          "type": "number",
          "description": "Survival time in seconds."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the score was achieved."
        },
        "uid": {
          "type": "string",
          "description": "User's unique ID."
        }
      },
      "required": ["name", "score", "timestamp", "uid"]
    },
    "UserProgress": {
      "title": "UserProgress",
      "description": "Stores user's badges and level progress.",
      "type": "object",
      "properties": {
        "badges": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of earned badge IDs."
        },
        "completedLevels": {
          "type": "array",
          "items": { "type": "number" },
          "description": "List of completed level numbers."
        }
      }
    }
  },
  "firestore": {
    "leaderboard": {
      "schema": "Score",
      "description": "Collection of top scores for Infinite Mode."
    },
    "users": {
      "schema": "UserProgress",
      "description": "User-specific progress data."
    }
  }
}
